# 文档审计与同步报告（2026-03-22）

> 历史快照：本报告反映 2026-03-22 当日状态；当前真值请以 `03-参考_Reference/00-源码静态分析与对齐基线.md` 为准。

## 1. 本次审计目标

按“代码真值源优先”对 `web_app` 完整代码库进行文档对齐，重点覆盖：

1. 前端架构
2. 后端逻辑
3. API 定义
4. 数据模型
5. 核心业务流转

## 2. 审计输入范围

1. 路由：`route/app.php`、`route/player.php`、`config/route.php`
2. 控制器：`app/*/controller/*.php` + `app/controller/*.php`
3. 模型与服务：`app/model/*`、`app/player/model/*`、关键 service
4. 数据库：`database/mhxy.sql`
5. 前端模板：`app/*/view/*`、`public/static/template/*`

## 3. 发现并修复的文档偏差

## 3.1 API 方法错误

- 文件：`docs/03-参考_Reference/02-API接口文档-Admin.md`
- 偏差：`/admin/player/role_table` 写为 `POST`
- 真值：控制器与页面均为 `GET`
- 处理：已修正并标注页面调用依据

## 3.2 兼容路由缺失

- 偏差：文档未覆盖下列生产兼容路径：
  - `/enlist/submit_code`
  - `/user/api/index.php/role/set|get`
  - `/user/api/index/role/set|get`
- 处理：已补入架构文档、功能模块文档与 SDK/API 文档

## 3.3 模块统计过时

- 文件：`docs/03-参考_Reference/06-功能模块说明.md`
- 偏差：`api` 控制器数量写为 10
- 真值：当前 `api/controller` 为 12（含 `Enlist`、`LegacyRole`）
- 处理：已修正并补充根应用 `Enlist` 兼容控制器

## 4. 新增文档

新增：

- `docs/03-参考_Reference/09-前后端架构与全链路实现审计.md`

用途：

1. 统一承载“前端架构 + 后端架构 + API + 数据模型 + 业务流转”的审计结论
2. 作为后续文档更新的索引基线

## 5. 已更新文件清单

1. `docs/03-参考_Reference/00-源码静态分析与对齐基线.md`
2. `docs/03-参考_Reference/01-项目架构说明.md`
3. `docs/03-参考_Reference/02-API接口文档-Admin.md`
4. `docs/03-参考_Reference/02-API接口文档-SDK.md`
5. `docs/03-参考_Reference/06-功能模块说明.md`
6. `docs/03-参考_Reference/README.md`
7. `docs/03-参考_Reference/09-前后端架构与全链路实现审计.md`（新增）

## 6. 维护建议（后续）

1. 每次路由或控制器新增时，同步更新 `00-源码静态分析与对齐基线.md`
2. 对“隐式路由可达”的动作，文档需明确标注“未显式注册但可访问”
3. 兼容层接口（LegacyRole/Enlist）应保留单独章节，避免被主链路文档遗漏

## 7. 第二阶段补充（教程/操作指南，2026-03-22）

本次继续完成 `01-教程_Tutorials` 与 `02-操作指南_How-To-Guides` 的深度清洗：

1. 重写教程：
   - `01-快速入门/02-首次部署.md`
   - `01-快速入门/03-基础使用.md`
   - `02-开发者指南/01-项目结构认知.md`
2. 重写操作指南：
   - `01-部署指南.md`
   - `02-配置指南.md`
   - `03-监控指南.md`
   - `04-故障排查.md`
3. 校正开发者资料中的过时示例：
   - `02-开发者指南/README.md`（链接口径）
   - `02-开发者指南/02-编码规范.md`（数据库表示例）
4. 批量修复失效内部链接与旧目录名引用。
5. 已执行本地 Markdown 链接检查，结果：`OK: no broken local markdown links`。

## 8. 第三阶段补充（解释/参考，2026-03-22）

本轮继续执行“源码真值对齐”，新增修正如下：

1. 解释目录：
   - 重写 `docs/04-解释_Explanation/01-安全修复记录总览.md`
   - 修正 `docs/04-解释_Explanation/README.md` 中不存在的 `archive/` 结构与失效引用
2. 参考目录：
   - 深度重写 `docs/03-参考_Reference/02-API接口文档-Agent.md`
   - 补齐 Agent 应用真实行为：维护关闭接口、提现条件、固定分成、订单可见范围、绑定列表 `selected` 分支、状态变更联动踢下线
   - 更新 `docs/03-参考_Reference/02-API接口文档-公共.md`，补充 `GET /api/index/index` 调试输出接口口径（非正式业务接口）
3. 全量链接抽检：
   - 对 `docs/*.md` 执行本地链接检查
   - 当前失效链接仅存在于历史执行报告（`文档优化执行报告_2026-02-28.md`）中的旧路径示例，不影响现行参考/教程/操作/解释目录导航

## 9. 第四阶段补充（API Login/Player 抽审，2026-03-22）

本轮完成 `Login` 与 `Player` 两份 API 文档的逐项抽审与修复：

1. 重写 `docs/03-参考_Reference/02-API接口文档-Login.md`
   - 明确“显式重定向优先 + 隐式路由兜底”的双轨路由口径
   - 补齐 `login/index/submit` 二次验证参数与 `code=99` 语义
   - 补齐历史发放接口签名字段（`prepareOp` 返回 `ts/sig`，执行时提交 `op_ts/op_sig`）
2. 重写 `docs/03-参考_Reference/02-API接口文档-Player.md`
   - 修正 `Feedback::submit` 参数口径（`role/content/csrf_token`，移除 `images` 误导）
   - 修正 `Role::getByServer` 参数名为 `server_id`
   - 补齐 `Profile::updatePassword` 的 `confirm_password` 与 CSRF 约束
   - 补齐 CDK/SendItem 别名路由与签名字段口径（`op_ts/op_sig`）
3. 本轮改动文件已执行本地链接检查，结果：无新增断链。
