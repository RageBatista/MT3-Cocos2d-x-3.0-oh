interface:
  display_name: "Windows v120 构建"
  short_description: "处理 Win32 v120 构建、链接错误与 ABI 兼容排障任务；不用于 Android/iOS 平台壳层或纯资源热更问题。"
  default_prompt: "使用 $windows-v120-build 分析当前 Win32 构建任务，并给出安全可执行的修复步骤。"
policy:
  allow_implicit_invocation: true
