interface:
  display_name: "Server Ant 构建"
  short_description: "处理服务端 Ant 构建、代码生成与打包发布流程；不用于客户端平台壳层或运行时业务逻辑问题。"
  default_prompt: "使用 $server-ant-build 分析当前服务端构建任务，并指出需要的生成步骤。"
policy:
  allow_implicit_invocation: true
