interface:
  display_name: "编码与 BOM 保护"
  short_description: "防止中文乱码、BOM 漂移与换行风格破坏的安全守卫；不负责业务逻辑判定。"
  default_prompt: "使用 $encoding-bom-guard 检查当前文件的编码、BOM 和回写方式。"
policy:
  allow_implicit_invocation: false
