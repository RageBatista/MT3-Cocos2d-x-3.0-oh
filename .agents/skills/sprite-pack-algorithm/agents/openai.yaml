interface:
  display_name: "精灵打包算法"
  short_description: "处理工作区本地 SpritePackCoreService、外部 pack.ini 与矩形打包；不用于热更新或运行时 UI。"
  default_prompt: "使用 $sprite-pack-algorithm 分析当前 SpriteEditor 打包任务，并定位算法与输出锚点。"
policy:
  allow_implicit_invocation: true
