interface:
  display_name: "渲染处理链"
  short_description: "处理运行时渲染器、绘制顺序、裁剪、批处理与 DrawCall；不用于 CEGUI XML 资源声明或平台壳层。"
  default_prompt: "使用 $rendering-pipeline 分析当前渲染链任务，并定位 UI、渲染器或特效实现。"
policy:
  allow_implicit_invocation: true
