interface:
  display_name: "Android r16/Ant 构建"
  short_description: "处理 Android NDK/Ant、JNI 原生编译与 APK 打包；不用于 JNI 注册回调或平台生命周期。"
  default_prompt: "使用 $android-r10e-build 分析当前 Android 构建任务，并指出正确的执行顺序。"
policy:
  allow_implicit_invocation: true
