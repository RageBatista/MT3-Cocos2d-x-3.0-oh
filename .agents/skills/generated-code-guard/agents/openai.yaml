interface:
  display_name: "生成代码保护"
  short_description: "防止误改 xbean、rpc、tolua++ 与 ProtoDef 生成产物；不直接承担业务实现。"
  default_prompt: "使用 $generated-code-guard 先判断当前任务是否命中生成代码边界。"
policy:
  allow_implicit_invocation: false
