interface:
  display_name: "平台桥接与抽象"
  short_description: "处理三端壳层、JNI 注册回调、ObjC++ 与 SDK 生命周期；不用于 NDK/Ant 构建或共享主链。"
  default_prompt: "使用 $platform-bridge 分析当前平台桥接任务，并定位入口、JNI 或 ObjC++ 锚点。"
policy:
  allow_implicit_invocation: true
