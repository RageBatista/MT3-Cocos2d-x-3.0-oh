# cocos2d-x 3.0-oh 网络、音频与物理模块

## 第一部分：网络模块 (network/)

**文件位置**: `cocos/network/`

### 1.1 HttpClient - HTTP客户端

```cpp
class HttpClient : public Ref {
public:
    static HttpClient* getInstance();
    static void destroyInstance();
    
    // 发送请求
    void send(HttpRequest* request);
    void sendImmediate(HttpRequest* request);
    
    // 超时
    void setTimeoutForConnect(int value);    // 连接超时(秒)
    void setTimeoutForRead(int value);       // 读取超时(秒)
    
    // Cookie
    void setCookie(const std::string& cookie);
    std::string getCookie() const;
    void clearCookies();
    
    // SSL/TLS
    void setSSLVerification(const std::string& caFile);
    void enableCookies(const char* cookieFile);
    
    // 内部基于 libcurl 实现
};
```

### 1.2 HttpRequest - HTTP请求

```cpp
class HttpRequest : public Ref {
public:
    enum class Type { GET, POST, PUT, DELETE, UNKNOWN };
    
    void setRequestType(Type type);
    Type getRequestType() const;
    
    void setUrl(const std::string& url);
    std::string getUrl() const;
    
    void setRequestData(const char* buffer, size_t len);
    char* getRequestData() const;
    ssize_t getRequestDataSize() const;
    
    void setTag(const std::string& tag);
    std::string getTag() const;
    
    void setResponseCallback(const std::function<void(HttpClient*, HttpResponse*)>& callback);
    
    // 请求头
    void setHeaders(std::vector<std::string> headers);
    std::vector<std::string> getHeaders() const;
};
```

### 1.3 HttpResponse - HTTP响应

```cpp
class HttpResponse : public Ref {
public:
    bool isSucceed() const;
    std::vector<char>* getResponseData() const;
    std::vector<char>* getResponseHeader() const;
    ssize_t getResponseCode() const;
    std::string getResponseHeaderString() const;
    HttpRequest* getHttpRequest() const;
};
```

### 1.4 WebSocket

```cpp
class WebSocket : public Ref {
public:
    enum class ErrorCode { TIME_OUT, CONNECTION_FAILURE, UNKNOWN };
    enum class State { CONNECTING, OPEN, CLOSING, CLOSED };
    
    WebSocket();
    ~WebSocket();
    
    bool init(const Delegate& delegate, const std::string& url,
              const std::vector<std::string>* protocols = nullptr,
              const std::string& caFilePath = "");
    void send(const std::string& message);
    void send(const unsigned char* binaryMsg, unsigned int len);
    void close();
    State getReadyState();
    
    // 委托接口
    class Delegate {
    public:
        virtual void onOpen(WebSocket* ws) = 0;
        virtual void onMessage(WebSocket* ws, const Data& data) = 0;
        virtual void onClose(WebSocket* ws) = 0;
        virtual void onError(WebSocket* ws, const ErrorCode& error) = 0;
    };
};
```

### 1.5 SocketIO

```cpp
class SocketIO : public Ref {
public:
    SocketIO();
    virtual ~SocketIO();
    
    static SocketIO* connect(const std::string& uri, const Delegate& delegate);
    void send(const std::string& event, const std::string& data);
    void emit(const std::string& event, const std::string& data);
    void close();
    void disconnect();
    
    class Delegate {
    public:
        virtual void onConnect(SIOClient* client) = 0;
        virtual void onMessage(SIOClient* client, const std::string& data) = 0;
        virtual void onClose(SIOClient* client) = 0;
        virtual void onError(SIOClient* client, const std::string& data) = 0;
    };
};
```

### 1.6 使用示例

```cpp
// HTTP GET请求
auto request = new HttpRequest();
request->setUrl("http://api.example.com/data");
request->setRequestType(HttpRequest::Type::GET);
request->setResponseCallback([](HttpClient* client, HttpResponse* response) {
    if (response->isSucceed()) {
        auto data = response->getResponseData();
        std::string str(data->begin(), data->end());
        CCLOG("Response: %s", str.c_str());
    }
});
HttpClient::getInstance()->send(request);
request->release();

// WebSocket
class MyDelegate : public WebSocket::Delegate {
    virtual void onOpen(WebSocket* ws) { CCLOG("Connected"); }
    virtual void onMessage(WebSocket* ws, const Data& data) { /* 处理消息 */ }
    virtual void onClose(WebSocket* ws) { CCLOG("Closed"); }
    virtual void onError(WebSocket* ws, const ErrorCode& error) { CCLOG("Error"); }
};
```

## 第二部分：音频模块 (audio/)

**文件位置**: `cocos/audio/`

### 2.1 CocosDenshion (传统音频引擎)

基于 OpenAL，支持 iOS/macOS/Win32/Android 平台。

```cpp
class SimpleAudioEngine {
public:
    static SimpleAudioEngine* getInstance();
    static void end();
    
    // 背景音乐
    void preloadBackgroundMusic(const char* pszFilePath);
    void playBackgroundMusic(const char* pszFilePath, bool bLoop = false);
    void stopBackgroundMusic(bool bReleaseData = false);
    void pauseBackgroundMusic();
    void resumeBackgroundMusic();
    void rewindBackgroundMusic();
    bool isBackgroundMusicPlaying();
    bool willPlayBackgroundMusic();
    float getBackgroundMusicVolume();
    void setBackgroundMusicVolume(float volume);
    
    // 音效
    unsigned int playEffect(const char* pszFilePath, bool bLoop = false,
                            float pitch = 1.0f, float pan = 0.0f, float gain = 1.0f);
    void pauseEffect(unsigned int nSoundId);
    void pauseAllEffects();
    void resumeEffect(unsigned int nSoundId);
    void resumeAllEffects();
    void stopEffect(unsigned int nSoundId);
    void stopAllEffects();
    void preloadEffect(const char* pszFilePath);
    void unloadEffect(const char* pszFilePath);
    float getEffectsVolume();
    void setEffectsVolume(float volume);
};
```

### 2.2 OHOS 音频引擎

**文件位置**: `cocos/audio/ohos/`

#### AudioEngine
```cpp
class AudioEngine {
public:
    static AudioEngine* getInstance();
    
    int play2d(const std::string& filePath, bool loop = false,
               float volume = 1.0f, const AudioProfile* profile = nullptr);
    void setVolume(int audioID, float volume);
    void setLoop(int audioID, bool loop);
    bool setCurrentTime(int audioID, float sec);
    void pause(int audioID);
    void resume(int audioID);
    void stop(int audioID);
    void stopAll();
    float getDuration(int audioID);
    float getCurrentTime(int audioID);
    bool setFinishCallback(int audioID, const std::function<void(int, const std::string&)>&);
    void uncache(const std::string& filePath);
    void uncacheAll();
    void preload(const std::string& filePath, const std::function<void(bool)>& callback = nullptr);
    
    // 内部组件
    // AudioDecoder - 音频解码器（支持多种格式）
    // AudioMixer - 音频混音器
    // Track - 音频轨道（播放单元）
    // PcmData - PCM数据封装
};
```

## 第三部分：物理模块 (physics/)

**文件位置**: `cocos/physics/`

### 3.1 PhysicsWorld - 物理世界

```cpp
class PhysicsWorld : public Ref {
public:
    void setGravity(const Vect& gravity);
    Vect getGravity() const;
    void setSpeed(float speed);
    float getSpeed() const;
    void setUpdateRate(int rate);
    int getUpdateRate() const;
    void setSubsteps(int steps);
    int getSubsteps() const;
    
    // 碰撞检测
    void setAutoStep(bool autoStep);
    void step(float delta);
    
    // 查询
    PhysicsShape* getShape(const Point& point) const;
    PhysicsBody* getBody(int tag) const;
    void removeBody(int tag);
    void removeAllBodies();
    
    // 碰撞掩码
    void setDebugDrawMask(int mask);
    int getDebugDrawMask() const;
    
    // 内部使用 Chipmunk2D 物理引擎
};
```

### 3.2 PhysicsBody - 物理刚体

```cpp
class PhysicsBody : public Ref, public Clonable {
public:
    static PhysicsBody* create();
    static PhysicsBody* create(float mass);
    static PhysicsBody* create(float mass, float moment);
    static PhysicsBody* createBox(const Size& size, PhysicsMaterial material = PHYSICS_MATERIAL_DEFAULT);
    static PhysicsBody* createCircle(float radius, PhysicsMaterial material = PHYSICS_MATERIAL_DEFAULT);
    static PhysicsBody* createEdgeBox(const Size& size, PhysicsMaterial material = PHYSICS_MATERIAL_DEFAULT);
    static PhysicsBody* createEdgeSegment(const Point& a, const Point& b, PhysicsMaterial material = PHYSICS_MATERIAL_DEFAULT);
    static PhysicsBody* createPolygon(const Point* points, int count, PhysicsMaterial material = PHYSICS_MATERIAL_DEFAULT);
    static PhysicsBody* createEdgeChain(const Point* points, int count, PhysicsMaterial material = PHYSICS_MATERIAL_DEFAULT);
    
    // 形状管理
    PhysicsShape* addShape(PhysicsShape* shape, bool addToWorld = true);
    void removeShape(PhysicsShape* shape, bool reduceMass = true);
    void removeAllShapes(bool reduceMass = true);
    Vector<PhysicsShape*>& getShapes();
    
    // 属性
    void setMass(float mass);
    float getMass() const;
    void setMoment(float moment);
    float getMoment() const;
    void setVelocity(const Vect& velocity);
    Vect getVelocity() const;
    void setAngularVelocity(float velocity);
    float getAngularVelocity() const;
    void setVelocityLimit(float limit);
    float getVelocityLimit() const;
    void setAngularVelocityLimit(float limit);
    float getAngularVelocityLimit() const;
    
    // 力
    void applyForce(const Vect& force, const Point& offset = Point::ZERO);
    void applyTorque(float torque);
    void applyImpulse(const Vect& impulse, const Point& offset = Point::ZERO);
    void applyAngularImpulse(float impulse);
    void resetForces();
    
    // 连接节点
    void setNode(Node* node);
    Node* getNode() const;
    
    // 类型
    void setDynamic(bool dynamic);
    bool isDynamic() const;
    void setRotationEnable(bool enable);
    bool isRotationEnabled() const;
    void setGravityEnable(bool enable);
    bool isGravityEnabled() const;
    
    // 标志
    void setCategoryBitmask(int bitmask);
    int getCategoryBitmask() const;
    void setContactTestBitmask(int bitmask);
    int getContactTestBitmask() const;
    void setCollisionBitmask(int bitmask);
    int getCollisionBitmask() const;
    
    // 碰撞回调
    void setContactTestBitmask(bool test);
    bool getContactTestBitmask() const;
    
    void removeFromWorld();
    Vect getPosition() const;
    float getRotation();
};
```

### 3.3 PhysicsShape - 物理形状

```cpp
class PhysicsShape : public Ref, public Clonable {
public:
    enum class Type { CIRCLE, BOX, POLYGON, EDGESEGMENT, EDGEBOX, EDGECHAIN, UNKNOWN };
    
    Type getType() const;
    float getArea() const;
    float getMass() const;
    void setMass(float mass);
    float getMoment() const;
    void setMoment(float moment);
    PhysicsBody* getBody() const;
    bool isSensor() const;
    void setSensor(bool sensor);
    
    // 碰撞回调
    void setGroup(int group);
    int getGroup() const;
    void setCategoryBitmask(int bitmask);
    int getCategoryBitmask() const;
    void setCollisionBitmask(int bitmask);
    int getCollisionBitmask() const;
    void setContactTestBitmask(int bitmask);
    int getContactTestBitmask() const;
};
```

### 3.4 PhysicsContact - 碰撞接触

```cpp
class PhysicsContact : public EventCustom {
public:
    enum class EventCode { NONE, BEGIN, PRESOLVE, POSTSOLVE, SEPARATE };
    
    PhysicsShape* getShapeA() const;
    PhysicsShape* getShapeB() const;
    PhysicsBody* getBodyA() const;
    PhysicsBody* getBodyB() const;
    PhysicsContact* getContact() const;
    bool isTouching() const;
    void setData(void* data);
    void* getData() const;
    EventCode getEventCode() const;
};
```

### 3.5 PhysicsJoint - 物理关节

```cpp
class PhysicsJoint : public Ref {
public:
    // 关节类型
    static PhysicsJoint* createFixed(PhysicsBody* a, PhysicsBody* b, const Point& anchr);
    static PhysicsJoint* createLimit(PhysicsBody* a, PhysicsBody* b, const Point& anchr1, const Point& anchr2, float min, float max);
    static PhysicsJoint* createPin(PhysicsBody* a, PhysicsBody* b, const Point& pivot);
    static PhysicsJoint* createDistance(PhysicsBody* a, PhysicsBody* b, const Point& anchr1, const Point& anchr2);
    static PhysicsJoint* createSpring(PhysicsBody* a, PhysicsBody* b, const Point& anchr1, const Point& anchr2, float stiffness, float damping);
    static PhysicsJoint* createGroove(PhysicsBody* a, PhysicsBody* b, const Point& grooveA, const Point& grooveB, const Point& anchr2);
    static PhysicsJoint* createRotarySpring(PhysicsBody* a, PhysicsBody* b, float stiffness, float damping);
    static PhysicsJoint* createRotaryLimit(PhysicsBody* a, PhysicsBody* b, float min, float max);
    static PhysicsJoint* createRatchet(PhysicsBody* a, PhysicsBody* b, float phase, float ratchet);
    static PhysicsJoint* createGear(PhysicsBody* a, PhysicsBody* b, float phase, float ratio);
    static PhysicsJoint* createMotor(PhysicsBody* a, PhysicsBody* b, float rate);
    
    void removeFormWorld();
    bool isEnabled() const;
    void setEnable(bool enable);
    void setMaxForce(float force);
    float getMaxForce() const;
};
```

### 3.6 使用示例

```cpp
// 创建物理场景
auto scene = Scene::createWithPhysics();
scene->getPhysicsWorld()->setGravity(Vect(0, -98.0f));
// 开启调试绘制
scene->getPhysicsWorld()->setDebugDrawMask(PhysicsWorld::DEBUGDRAW_ALL);

// 创建物理刚体
auto body = PhysicsBody::createBox(Size(100, 100));
body->setDynamic(true);
body->setCategoryBitmask(0x01);
body->setCollisionBitmask(0x02);
body->setContactTestBitmask(0xFFFFFFFF);

auto sprite = Sprite::create("box.png");
sprite->setPhysicsBody(body);
scene->addChild(sprite);

// 碰撞监听
auto contactListener = EventListenerPhysicsContact::create();
contactListener->onContactBegin = [](PhysicsContact& contact) {
    CCLOG("Contact begin!");
    return true;
};
_eventDispatcher->addEventListenerWithSceneGraphPriority(contactListener, scene);
```

## 第四部分：存储模块 (storage/)

**文件位置**: `cocos/storage/`

```cpp
class LocalStorage {
public:
    static void setItem(const std::string& key, const std::string& value);
    static std::string getItem(const std::string& key);
    static void removeItem(const std::string& key);
    static void clear();
};
```