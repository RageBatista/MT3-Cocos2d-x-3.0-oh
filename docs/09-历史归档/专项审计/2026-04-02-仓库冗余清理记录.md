# 2026-04-02 仓库冗余盘点与清理时间线

> **状态**: 历史快照
> **适用日期**: 2026-04-02 清理前盘点、批次确认与执行
> **当前基线**:
> - [MT3 文档中心](../../README.md)
> - [文档索引](../../07-参考文档/02-文档索引.md)
> - [项目架构](../../02-技术架构/02-项目架构.md)
> **证据边界**: 阶段一完整保存候选分级、13.93 GB 估算和高风险保留项；阶段二完整保存 A/B/C 执行批次、空间变化和验证结果。本文只表示 2026-04-02 当日状态，不表示当前工作区仍具备相同目录或空间占用。

## 1. 时间线总览

| 阶段 | 当日事实 |
| --- | --- |
| 清理前盘点 | 低风险候选 15 个目录、20,755 个文件、14,263.39 MB（约 13.93 GB）；`cache/`、已跟踪 IDE 文件和未引用代码候选列为高风险保留项。 |
| 批次 A | 删除 Android build/obj、Win32 intermediates 与日志目录；磁盘实际回收约 13.97 GB。 |
| 批次 B | 清理 `build/` 历史内容、旧构建审计目录和 SuperLJFilePackUnpack 的未跟踪构建项；额外回收约 4.48 GB。 |
| 批次 C | 清理未跟踪 `.bak/.log/.suo/.tmp/.temp`；释放 41.84 MB。 |
| 累计结果 | 删除 130,693 个文件，报告口径释放 18,708.31 MB；磁盘空闲由 7.71 GB 提升到 26.20 GB。 |
| 明确保留 | `cache/`、已被 Git 跟踪的 `*.sdf/*.ipch/*.aps/*.orig/*.rej`、供应商/补丁高风险残留和“未引用代码文件”专项候选。 |

## 2. 阶段一：清理前盘点与候选分级

> **原始路径**: `docs/audit/redundancy-audit-2026-04-02.md`
> **原始 SHA256**: `36FC9AF29E0A1574D48F81E9B7CDAE3AA8B1A118B170F5E80360B711FCA6D8C3`

### 1. 范围与方法

- 盘点范围：`E:\MT3` 全仓。
- 扫描方法：目录体积统计、可疑扩展名统计、Git 跟踪状态核对、构建/缓存目录分类。
- 当前阶段：**只生成清单，不执行删除**。

### 2. 当前风险前提

- 仓库当前存在大量活动变更：
  - 已跟踪改动约 `42145` 项
  - 未跟踪文件约 `672757` 项
- 因此本次清理必须遵守两条边界：
  - 只把“高置信度中间产物/日志/缓存/备份”列为低风险候选
  - 对已被 Git 跟踪或仍在当天写入的路径，默认不删

### 3. 低风险候选（建议优先确认）

这些路径都满足以下至少一条：

- 路径本身未被 Git 跟踪
- 内容全部或绝大多数为 ignored/build 产物
- 目录名与内容都符合标准中间产物特征

| 路径 | 文件数 | 体积(MB) | 最近写入时间 | Git 状态 | 说明 |
|---|---:|---:|---|---|---|
| `client/android/LocojoyProject64/build` | 388 | 5326.78 | 2026-03-11 01:08 | 未跟踪路径 / 388 ignored | Android 渠道构建产物 |
| `client/android/LocojoyProject/build` | 10498 | 1877.42 | 2026-03-30 22:03 | 未跟踪路径 / 10498 ignored | Android 渠道构建产物 |
| `client/android/LocojoyProject64/obj64` | 1850 | 1263.89 | 2026-03-10 19:39 | 未跟踪路径 / 1850 untracked | Android NDK 中间产物 |
| `client/android/LocojoyProject/obj` | 1828 | 877.75 | 2026-03-30 22:00 | 未跟踪路径 / 1828 ignored | Android NDK 中间产物 |
| `client/android/LocojoyProject64/obj` | 1828 | 877.39 | 2026-03-11 01:05 | 未跟踪路径 / 1828 ignored | Android NDK 中间产物 |
| `client/android/YijieProject/obj` | 1840 | 877.29 | 2026-03-05 14:04 | 未跟踪路径 / 1840 ignored | Android NDK 中间产物 |
| `client/android/JoysdkProject/obj` | 1839 | 877.26 | 2026-03-05 13:59 | 未跟踪路径 / 1839 ignored | Android NDK 中间产物 |
| `client/MT3Win32App/Debug.win32` | 147 | 781.63 | 2026-03-31 00:52 | 未跟踪路径 / 147 ignored | Win32 Debug 中间产物 |
| `client/MT3Win32App/Release.win32` | 151 | 595.86 | 2026-03-31 10:20 | 未跟踪路径 / 151 ignored | Win32 Release 中间产物 |
| `client/FireClient/Debug.win32` | 29 | 578.22 | 2026-03-03 13:47 | 未跟踪路径 / 29 ignored | FireClient Debug 中间产物 |
| `client/FireClient/Release.win32` | 22 | 237.47 | 2026-03-31 10:20 | 未跟踪路径 / 22 ignored | FireClient Release 中间产物 |
| `tools/engine/build_logs` | 29 | 73.37 | 2026-03-07 14:51 | 未跟踪路径 / 24 ignored | 工具构建日志 |
| `client/resource/tools/log` | 23 | 14.22 | 2026-04-01 15:50 | 未跟踪路径 / 23 ignored | 资源工具运行日志 |
| `client/android/JoysdkProject/build` | 148 | 2.92 | 2026-03-04 14:11 | 未跟踪路径 / 148 ignored | Android 渠道构建产物 |
| `client/android/YijieProject/build` | 135 | 1.92 | 2026-03-04 14:11 | 未跟踪路径 / 135 ignored | Android 渠道构建产物 |

#### 低风险候选合计

- 合计文件数：`20755`
- 合计体积：`14263.39 MB`（约 `13.93 GB`）

### 4. 中风险候选（建议单独确认）

#### 4.1 历史构建/拆包工作目录

| 路径 | 文件数 | 体积(MB) | 最近写入时间 | 风险说明 |
|---|---:|---:|---|---|
| `build` | 107169 | 3812.33 | 2026-04-02 09:40 | 已完成历史内容清理；但后台进程持续在该目录重建少量未跟踪 CMake 文件，当前残留不足 `0.3 MB` |
| `tools/reports/build-audit-20260324-221048` | 83 | 553.68 | 2026-03-25 02:00 | 已删除 |
| `dependencies/SuperLJFilePackUnpack/build` | 177 | 37.38 | 2026-04-02 11:41 | 已完成选择性清理，仅保留被 Git 跟踪的工程文件及少量混合 `CMakeFiles` 内容 |

#### 4.2 `build/` 内部结构说明

- `build/西游0.1折`：约 `1959.71 MB`
  - 结构包含 `assets/`、`smali/`、`lib/`、`original/`、`apktool.yml`
  - 更像 APK 拆包产物，而不是普通编译缓存
- `build/主宰西游`：约 `1852.37 MB`
  - 同样是 APK 拆包结构
  - 含 `assets/unpacked/` 二次解包内容
  - 存在 `lib/armeabi-v7a/libgame.so.bak`

结论：`build/` 已在用户确认后完成历史内容清理；当前残留是后台工具重新生成的一小批未跟踪 CMake 产物，不再是原始拆包内容。

### 5. 高风险候选（默认不动）

#### 5.1 当前活跃分析缓存

| 路径 | 文件数 | 体积(MB) | 最近写入时间 | 处理建议 |
|---|---:|---:|---|---|
| `cache` | 1383 | 89.58 | 2026-04-02 20:42 | 不删。你当前打开的 `cache/gbeans_*` 文件就在该工作流内，属于活跃分析产物 |

#### 5.2 已被 Git 跟踪的 IDE/中间文件

| 模式 | 数量 | 体积(MB) | 其中被 Git 跟踪 |
|---|---:|---:|---:|
| `*.sdf` | 22 | 1441.73 | 15 |
| `*.ipch` | 5 | 465.88 | 7 |
| `*.aps` | 5 | 0.26 | 6 |
| `*.orig` | 2 | 0.03 | 2 |
| `*.rej` | 1 | 0.00 | 1 |

代表路径：

- `client/FireClient/FireClient.sdf`
- `client/Launcher/Launcher.sdf`
- `client/FireClient/ipch/fireclient.win32-88927c8a/fireclient-6ad1ca6c.ipch`
- `tools/CELayoutEditor-0.7.1/vc++12/ipch/celayouteditor-58d03a7/celayouteditor_d-2a70cc9c.ipch`
- `tools/PluginCheck/ipch/getattributecode-64b45864/getattributecode-5772aa7b.ipch`
- `client/Launcher/Launcher.aps`
- `tools/engine/pfs/tools/downloader/downloader.aps`

结论：这些文件虽然形态上像“垃圾文件”，但既然已经进入版本库，本次不能作为低风险清理项，需要后续作为一次单独的仓库治理任务处理。

#### 5.3 备份与补丁残留

| 模式 | 数量 | 体积(MB) | 说明 |
|---|---:|---:|---|
| `*.bak` | 0 | 0.00 | 已完成清理 |
| `*.orig` | 2 | 0.03 | 补丁残留 |
| `*.rej` | 1 | 0.00 | 补丁冲突残留 |

结论：未跟踪的 `.bak` 已在后续清理中删除；被版本库记录的 `.orig/.rej` 继续保留。

### 6. “未引用代码文件”审计结论

- 当前只做了**高保守版筛查**：检查未被 Git 跟踪、且扩展名像源码/脚本/配置的文件。
- 结果：约 `2065` 个“代码样式”未跟踪文件。
- 主要分布：
  - `dependencies/third-party-rebuild`：716
  - `cache/auto`：379
  - `server/server`：336
  - `tools/free-tex-packer`：68
  - `client/android`：56
  - `tools/LJFilePackUnpacker`：54
  - `cache/gbeans_production_ready`：47
  - `cache/gbeans_recovered`：46
  - `cache/gbeans_for_auto`：42

判定：

- 这些文件大多数是第三方重建工程、生成物、分析缓存、脚本或文档，不属于“可以直接当未引用代码删除”的范围。
- 真正的“未被工程/脚本/运行链引用的源码文件”需要进一步按子系统解析：
  - Win32：`.vcxproj/.filters`
  - Android：`Android.mk`、Ant 工程目录
  - 服务器：`build.xml`、生成链入口
  - Lua/UI：脚本装载链与资源索引

因此，这一类本次只记录为**需要第二阶段专项分析**，不纳入本轮低风险删除清单。

### 7. 建议的确认批次

#### 批次 A：可先执行的低风险清理

- 删除第 3 节全部目录
- 预估释放：约 `13.93 GB`
- 风险：需要重新构建时会重新生成

#### 批次 B：需要你额外确认后再动

- 已执行：
  - `build/` 内容清理
  - `tools/reports/build-audit-20260324-221048`
  - `dependencies/SuperLJFilePackUnpack/build` 中的非跟踪子目录与日志
- 仍保留：
  - `build/` 空目录壳（被外部进程占用）
  - `dependencies/SuperLJFilePackUnpack/build/CMakeFiles`

#### 批次 C：本轮明确保留

- `cache/`
- 所有已被 Git 跟踪的 `*.sdf/*.ipch/*.aps/*.orig/*.rej`
- 供应商树中的大批量 `.bak`
- “未引用代码文件”候选

### 8. 本轮建议动作

建议先按下面顺序执行：

1. 仅清理批次 A
2. 校验 `git status`、路径删除结果与空间回收
3. 再决定是否处理批次 B

## 3. 阶段二：确认后的清理执行与验证

> **原始路径**: `docs/audit/redundancy-cleanup-report-2026-04-02.md`
> **原始 SHA256**: `C912CF592131160996CBF21A99C0FBBE72C963A74FCAFCD239DCC73E9837D748`

### 1. 执行范围

本次已执行三批清理。

#### 批次 A

- Android 构建产物目录
- Android `obj/obj64` 中间产物目录
- Win32 `Debug.win32/Release.win32` 中间产物目录
- 工具与资源日志目录

未执行：

- `cache/`
- 已被 Git 跟踪的 `*.sdf` / `*.ipch` / `*.aps`
- “未引用代码文件”候选

#### 批次 B

- 清空 `build/` 内容
- 删除 `tools/reports/build-audit-20260324-221048`
- 选择性清理 `dependencies/SuperLJFilePackUnpack/build` 中 `Tracked=0` 的子目录、日志和输出物

#### 批次 C

- 删除所有未跟踪 `*.bak`
- 删除所有未跟踪 `*.log`
- 删除所有未跟踪 `*.suo` / `*.tmp` / `*.temp`

### 2. 清理结果

#### 2.1 按类型汇总

| 类型 | 文件数 | 释放空间(MB) |
|---|---:|---:|
| Android build | 11169 | 7209.04 |
| Android obj | 9185 | 4773.58 |
| Win32 intermediates | 349 | 2193.18 |
| Logs | 52 | 87.59 |

#### 2.2 总计

- 批次 A：
  - 删除目录数：`15`
  - 删除文件数：`20755`
  - 释放空间：`14263.39 MB`
  - 按磁盘空闲变化测得的实际回收：约 `13.97 GB`
- 批次 B：
  - 删除文件数：`107380`
  - 释放空间：`4403.08 MB`
  - 从批次 A 后到批次 B 完成，磁盘空闲由 `21.68 GB` 提升到 `26.16 GB`，额外回收约 `4.48 GB`
- 批次 C：
  - 删除文件数：`2558`
  - 释放空间：`41.84 MB`
  - 磁盘空闲由 `26.16 GB` 提升到 `26.20 GB`
- 累计：
  - 删除文件数：`130693`
  - 释放空间：`18708.31 MB`
  - 磁盘空闲由 `7.71 GB` 提升到 `26.20 GB`

### 3. 已删除目录

- `client/android/LocojoyProject64/build`
- `client/android/LocojoyProject/build`
- `client/android/LocojoyProject64/obj64`
- `client/android/LocojoyProject/obj`
- `client/android/LocojoyProject64/obj`
- `client/android/YijieProject/obj`
- `client/android/JoysdkProject/obj`
- `client/MT3Win32App/Debug.win32`
- `client/MT3Win32App/Release.win32`
- `client/FireClient/Debug.win32`
- `client/FireClient/Release.win32`
- `tools/engine/build_logs`
- `client/resource/tools/log`
- `client/android/JoysdkProject/build`
- `client/android/YijieProject/build`

### 4. 校验结果

- 15 个目标目录均已不存在。
- 定向检查本次清理路径下的 Git 已跟踪删除项，结果为：`0`。
- 全仓 `git ls-files --deleted` 仍存在历史删除项，但与本次清理路径无关，属于已有脏工作区状态。
- `tools/reports/build-audit-20260324-221048` 已不存在。
- `dependencies/SuperLJFilePackUnpack/build` 中本次选定的 `Tracked=0` 子项已不存在。
- 所有未跟踪 `*.bak` / `*.log` / `*.suo` / `*.tmp` / `*.temp` 已清零。
- `build/` 的历史内容已清空，但后台进程持续重新生成少量未跟踪 CMake 文件；当前残留不足 `0.3 MB`。

### 5. 对功能影响的判断

- 本次删除内容均为构建中间产物、渠道构建输出或日志。
- 不影响源码、脚本、配置、资源定义和当前活跃 `cache/gbeans_*` 分析工作目录。
- 后续若需要重新编译 Android/Win32，对应目录会自动重新生成。

### 6. 后续建议

若继续清理，下一批建议单独确认：

1. 关闭可能持续生成 `build/` CMake 文件的后台工具后，再删 `build/` 根目录与其新生内容
2. 评估 `dependencies/SuperLJFilePackUnpack/build/CMakeFiles` 内剩余的混合 tracked/untracked 内容
3. 继续处理高风险保留项：`cache/`、已跟踪的 IDE 中间文件、被版本库保留的 `.orig/.rej`、未引用代码专项分析

## 4. 当日最终边界

- 已执行项以阶段二的批次 A/B/C、文件数、空间和校验结果为准。
- 高风险保留项未纳入当日删除；后续处理需重新盘点，不能沿用 2026-04-02 的目录占用数字直接操作。
- `build/` 当日存在后台工具重新生成少量 CMake 文件的现象，因此“历史内容已清空”与“目录绝对为空”不是同一结论。
