# D2 执行单 - P0-006 参数开关冒烟

> **状态**: 历史快照
> **适用日期**: 2026-03-06 P0 执行批次
> **当前基线**:
> - [MT3 文档中心](../../../README.md)
> - [文档索引](../../../07-参考文档/02-文档索引.md)
> - [文档维护指南](../../../10-管理文档/01-文档维护指南.md)
> **说明**: 本文为 P0-006 参数开关冒烟执行单，保留当次执行证据，不直接替代当前冒烟测试总流程。


## 1. 任务信息

| 字段 | 内容 |
|------|------|
| 任务ID | P0-006 |
| 责任角色 | QA-Func |
| 协作角色 | INT, RD-Engine |
| 执行日期 | 2026-03-06 |
| 状态 | 已完成（Win32+Android 通过，iOS 延期） |

## 2. 冒烟范围

1. 校验 `[P0][BOOT]` 日志字段完整性与格式稳定性。
2. 校验 Win32 重复启动后参数值一致性。
3. 校验 Android 参数与 Win32 对齐。
4. iOS 冒烟按执行决策延期，不作为 D2 阻塞。

## 3. Win32 冒烟结果

| 用例ID | 用例描述 | 结果 | 证据 |
|------|------|------|------|
| SMK-001 | 启动日志包含 `[P0][BOOT]` | 通过 | `client/resource/bin/Release/startup_bootstrap.log` |
| SMK-002 | 参数字段齐全（`multiSampleType`、`bEnableMipMap`、`dwRenderFlags`、`appInitStep`） | 通过 | 同上 |
| SMK-003 | 两轮启动参数值一致 | 通过 | `00:32:16` 与 `00:39:38` 两条日志值一致 |

## 4. Android 冒烟结果

| 用例ID | 用例描述 | 结果 | 证据 |
|------|------|------|------|
| SMK-004 | 启动日志包含 `[P0][BOOT]` | 通过 | `adb logcat -d`（MuMu `192.168.5.218:5555`） |
| SMK-005 | 参数字段齐全（`multiSampleType`、`bEnableMipMap`、`dwRenderFlags`、`appInitStep`） | 通过 | `03-05 12:47:00.226 I mt3 : [P0][BOOT] ...` |
| SMK-006 | Android 参数与 Win32 对齐 | 通过 | 两端均为 `4/0/0x00000001/8` |

## 5. 延期项

| 平台 | 待执行项 | 当前状态 |
|------|------|------|
| iOS | 采样并验证 `[P0][BOOT]` 字段 | 延期（按执行决策跳过） |

## 6. 当前结论

1. Win32 与 Android 侧参数开关冒烟可通过。
2. `P0-006` 在本轮执行范围内收口，iOS 项转后续专项补齐。

## 7. 自动化支撑

1. Win32 自动采样脚本：`tools/scripts/Collect-P0Boot-Win32.ps1`
2. Android 自动采样脚本：`tools/scripts/Collect-P0Boot-Android.ps1`
3. 自动回填 CSV：[p0_boot_capture_records.csv](p0_boot_capture_records.csv)
