# D2 执行单 - P0-005 三端参数一致性联调

> **状态**: 历史快照
> **适用日期**: 2026-03-06 P0 执行批次
> **当前基线**:
> - [MT3 文档中心](../../../README.md)
> - [文档索引](../../../07-参考文档/02-文档索引.md)
> - [文档维护指南](../../../10-管理文档/01-文档维护指南.md)
> **说明**: 本文为 P0-005 三端参数一致性联调执行单，保留当次执行证据，不直接替代当前联调总流程。


## 1. 任务信息

| 字段 | 内容 |
|------|------|
| 任务ID | P0-005 |
| 责任角色 | INT |
| 协作角色 | RD-Engine, QA-Func |
| 执行日期 | 2026-03-06 |
| 状态 | 已完成（按执行决策跳过 iOS 采样） |

## 2. 联调目标

1. Win32、Android 启动阶段输出同格式 `[P0][BOOT]` 日志并完成参数对齐。
2. 输出参数一致性结论与差异项，作为 `P0-006` 冒烟输入。
3. iOS 采样按执行决策延期，不作为 D2 阻塞项。

## 3. 三端入口确认

| 平台 | 入口文件 | 关键调用 |
|------|------|------|
| Win32 | `client/MT3Win32App/main.cpp` | `gRunGameApplication()` |
| iOS | `client/FireClient/FireClient/FireClientAppDelegate.mm` | `gRunGameApplication()` |
| Android | `client/android/*/jni/main.cpp` | `gRunGameApplication()` |

## 4. 日志比对口径

固定检索关键词：
1. `[P0][BOOT]`

固定日志格式（保持不变）：
1. `[P0][BOOT] multiSampleType=%d bEnableMipMap=%d dwRenderFlags=0x%08X appInitStep=%d`

建议抓取命令：
1. Win32：`rg -n "\\[P0\\]\\[BOOT\\]" <startup_bootstrap.log>`
2. Android：`adb logcat | grep "\\[P0\\]\\[BOOT\\]"`
3. iOS：`xcrun simctl spawn booted log stream --level debug | grep "\\[P0\\]\\[BOOT\\]"`

## 5. 联调记录表

| 平台 | 日志路径/来源 | multiSampleType | bEnableMipMap | dwRenderFlags | appInitStep | 结论 |
|------|------|------|------|------|------|------|
| Win32 | `client/resource/bin/Release/startup_bootstrap.log` | 4 | 0 | 0x00000001 | 8 | 通过 |
| iOS | 无（本轮按执行决策跳过） | - | - | - | - | 延期 |
| Android | `adb logcat -d`（MuMu `192.168.5.218:5555`） | 4 | 0 | 0x00000001 | 8 | 通过 |

## 6. 差异归因模板

| 差异项 | 平台 | 现象 | 初步原因 | 责任角色 | 处理状态 |
|------|------|------|------|------|------|
|  |  |  |  |  |  |

## 7. 当前进展（2026-03-06 02:21）

1. 三端入口已确认，统一落到 `gRunGameApplication()`。
2. `[P0][BOOT]` 日志格式已固定并可用于比对。
3. Win32 已完成两轮实跑采样并回填参数记录（两次值一致）。
4. Android 已完成实跑采样并回填参数记录（MuMu 启动后自动采样通过）。
5. iOS 采样已调整为延期项，不阻塞 D3 执行。

## 8. 执行决策记录（2026-03-06 02:21）

1. iOS 工具链探测：`where xcrun` 未找到命令（当前为 Windows 环境）。
2. 经项目执行决策，D2 阶段跳过 iOS `[P0][BOOT]` 采样，后续按 iOS 专项阶段补齐。
3. Android 已完成采样：`adb devices -l` 识别 MuMu 设备 `192.168.5.218:5555`，参数通过。

## 9. 自动采样与回填

自动采样脚本：
1. Win32：`tools/scripts/Collect-P0Boot-Win32.ps1`
2. Android：`tools/scripts/Collect-P0Boot-Android.ps1`

自动回填 CSV：
1. [`p0_boot_capture_records.csv`](p0_boot_capture_records.csv)

已执行结果（最新）：
1. Win32：`pass`，参数 `4/0/0x00000001/8`。
2. Android：`pass`，参数 `4/0/0x00000001/8`（来源 `adb logcat -d`）。
3. iOS：`deferred`，依据“跳过 iOS 采样补齐”执行决策。
