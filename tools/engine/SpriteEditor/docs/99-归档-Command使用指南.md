# 归档文档：Command 模式使用指南

> **归档状态**: 仅供参考，不代表当前覆盖范围  
> **处理日期**: 2026-04-23  
> **当前事实源**: `CommandManager.h`、`SpriteEditorView.cpp`、`docs/04-API参考.md`

---

## 文档定位

本文件记录了某次任务阶段中关于 Command 模式的专项说明，属于**历史专题指南**。

## 当前适用性

- 可作为理解 `CommandManager` 使用意图的补充背景
- 可帮助快速理解部分可撤销操作的接入方式
- 不可据此推断“所有编辑操作均已纳入 Command 系统”

## 当前维护结论

当前真实状态是：

- `CommandManager` 已进入主线
- 但只覆盖部分编辑路径
- 具体覆盖范围应以当前源码中的 `ExecuteCommand()` 调用点为准
